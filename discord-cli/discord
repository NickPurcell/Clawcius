#!/usr/bin/env python3
"""Run the CLI straight from a checkout, with no install step."""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from dcli.cli import entrypoint  # noqa: E402

if __name__ == "__main__":
    entrypoint()
