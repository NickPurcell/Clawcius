# Clawcius — instance 1. An instance file holds facts: `crew`, `displayName` and the channel lists.
# Prompt content and every derived path or identity are refused here by the loader.
extends: agent-config.base.yaml

crew: clawcius
displayName: Clawcius

discord:
  allowedChannelIds:
    - '1105739162230984735'  # clanker-internet-asbestos-gen
  followUpChannelIds:
    - '1105739162230984735'
  # A channel here wakes the crew on every message; it must also be in allowedChannelIds.
  alwaysOnChannelIds: []
