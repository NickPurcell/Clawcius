# OJ

A pull request reviewer: a long-running orchestrator (OJO) that watches GitHub
repositories and wakes a headless Claude Code session per pull request to
review it.

This branch is the baseline. The implementation arrives by pull request, so
that the first version of a code-review bot is itself something you review.
