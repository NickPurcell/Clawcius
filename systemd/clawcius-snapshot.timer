[Unit]
Description=Periodic snapshot of the Clawcius agent container

[Timer]
OnCalendar=*-*-* 04:00:00
# Catch up after downtime rather than silently skipping a day.
Persistent=true
RandomizedDelaySec=15m

[Install]
WantedBy=timers.target
