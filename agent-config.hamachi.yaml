# Hamachi — instance 2, SimpleTestServer. agent-config.yaml says what an instance file holds.
extends: agent-config.base.yaml

crew: hamachi
displayName: Hamachi

discord:
  allowedChannelIds:
    - '1467070145343258628'  # test channel
  followUpChannelIds:
    - '1467070145343258628'
  alwaysOnChannelIds:
    - '1467070145343258628'

# Sessions run on the host as the hamachi account, not in a container.
container:
  enabled: false
